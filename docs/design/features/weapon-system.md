# 武器槽与扩展系统

## 概述

玩家**没有背包**。所有战斗道具通过**武器槽**与**扩展槽**管理。核心自定义发生在单把武器及其扩展的组合上。

## 武器槽

| 参数 | 值 | 可配置 |
|------|-----|--------|
| 默认武器数 | 1 | — |
| 最大武器数 | 6 | 是 |
| 初始武器 | 由职业/开局配置指定 | 是 |

- 每把武器在战斗中**独立自动攻击**（或共享冷却，由武器模板决定）。
- 武器槽满时，只能通过**合成**或**替换**（若实现）腾出空间。

## 扩展槽

每把武器拥有 **6~9 个扩展槽**（具体数量由武器模板或全局配置决定）。

| 配置项 | 说明 |
|--------|------|
| `extension_slot_min` | 全局下限，如 6 |
| `extension_slot_max` | 全局上限，如 9 |
| `slots_per_weapon` | 单武器覆盖，如步枪 8、手枪 6 |

扩展分为：

| 类型 | 作用范围 | 示例 |
|------|----------|------|
| 武器扩展 | 仅该武器 | 双发、穿透弹、燃烧命中 |
| 全局 Buff 扩展 | 玩家全局 | 移速 +10%、经验拾取范围 |
| 武器 BD 扩展 | 该武器流派构筑 | 「电弧」套装 2/4 件效果 |

## 武器数据结构（概念）

```json
{
  "instance_id": "w_001",
  "weapon_type": "pulse_rifle",
  "quality": 3,
  "base_stats": { "damage": 12, "fire_rate": 1.2 },
  "extensions": [
    { "ext_id": "double_shot", "stack": 1 },
    { "ext_id": "burn_on_hit", "stack": 2 }
  ],
  "execution_override": null
}
```

- `weapon_type`：合成与商店去重依据。
- `quality`：品质档位 1~6，影响底图色与数值系数。
- `extensions`：已挂载扩展列表；相同扩展可叠层（若设计允许）。

## 合成规则

**条件**：两把武器的 `weapon_type` 相同，且 `quality` 相同。

**结果**：

| 项目 | 规则 |
|------|------|
| 槽位 | 2 → 1，释放 1 个武器槽 |
| 品质 | 不变（同品质合成） |
| 基础数值 | 按公式提升，如 `damage × 1.25`，`fire_rate × 1.1` |
| 扩展 | **主武器**保留；副武器扩展按「同 ID 合并 stack，否则丢弃」或「全部合并到主武器（槽位满则按优先级丢弃」— 首版建议：同 ID 叠层，其余丢弃 |

**不可合成**：类型不同或品质不同 → UI 灰显。

后续可扩展：**同品质合成 N 次后升品**（非首期范围）。

## 商店与武器的关系

- 购买**新武器** → 填入第一个空武器槽。
- 购买**武器扩展** → 玩家选择目标武器与扩展槽位（或自动填入第一个空扩展槽）。
- **全局 Buff** → 建议占用武器的「全局扩展槽」或独立玩家级槽位（见商店文档），避免与武器 BD 抢槽。

## 与攻击序列的关系

武器模板定义**默认执行序列**；部分扩展可：

- 插入新步骤（如 `on_hit` 追加爆炸）
- 修改步骤参数（如 `projectile.count + 2`）
- 替换整个序列分支（稀有扩展）

详见 [weapon-execution-sequence.md](weapon-execution-sequence.md)。

## 配置建议（project / data）

```ini
# 示例：游戏平衡配置
weapon_slots_max = 6
weapon_slots_default = 1
extension_slots_default = 7
extension_slots_min = 6
extension_slots_max = 9
```

单武器可在 `weapons/pulse_rifle.tres` 中覆盖 `extension_slot_count = 8`。
